# Quality DB (Docker)
DATABASE_URL="postgresql://quality:quality@localhost:5433/quality_db"

# External scrapers DB (read-only, via kubectl port-forward on 5434)
# Run: npm run scrapers-db  (forwards Kubernetes service to localhost:5434)
SCRAPERS_DATABASE_URL="postgresql://user:pass@localhost:5434/scrapers_db"

# AI Gateway
AI_BASE_URL="https://ai.groupbwt.dev/v1"
AI_AUTH_TOKEN="sk-..."
AI_MODEL="minimax/MiniMax-M3"
