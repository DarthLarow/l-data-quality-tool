# Quality DB (Docker)
DATABASE_URL="postgresql://quality:quality@localhost:5433/quality_db"

# External scrapers DB (read-only, via kubectl port-forward)
# Stage: npm run scrapers-db:stage  → localhost:5435
# Prod:  npm run scrapers-db:prod   → localhost:5434
SCRAPERS_DB_HOST="localhost"
SCRAPERS_DB_PORT="5435"
SCRAPERS_DB_NAME=""
SCRAPERS_DB_USER=""
SCRAPERS_DB_PASSWORD=""

# AI Gateway
AI_BASE_URL="https://ai.groupbwt.dev/v1"
AI_AUTH_TOKEN="sk-..."
AI_MODEL="minimax/MiniMax-M3"
